# Gmail (OAuth installed-app; el token se genera una vez localmente)
GMAIL_TOKEN_JSON=        # contenido del token.json con refresh_token

# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_KEY=    # service_role (solo backend/cron)

# Claude API (categorización)
ANTHROPIC_API_KEY=

# Config
FEL_SENDER=notificacionesfel@sat.gob.gt
BACKFILL_MONTHS=12       # 0 = solo de hoy en adelante
IA_MODEL=claude-haiku-4-5-20251001
