# Secrets — nunca commitear
.env
token.json
credentials.json

# Fixtures con datos reales de facturas (PII) — repo público, NO subir.
# El .gitkeep sí se trackea para conservar la carpeta.
tests/fixtures/*.html
tests/fixtures/raw/

# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
venv/

# Dashboard (Next.js)
dashboard/node_modules/
dashboard/.next/

# OS
.DS_Store
